# Importing the random module
import random

# Getting names as input from the user
names_string = input("Give me everybody's names, separated by a comma: ")

# Splitting the names into a list
names = names_string.split(",")

# Counting the number of items in the list
num_items = len(names)

# Generating a random index to select a name from the list
random_choice = random.randint(0, num_items - 1)

# Selecting a name from the list based on the random index
person_who_will_pay = names[random_choice]

# Printing the selected person who will pay for the bills
print(person_who_will_pay + " is going to buy the meal today")
